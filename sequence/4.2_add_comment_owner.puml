@startuml 4.2 User add a comment to a music (client - owner)

actor Server as server

box "Client App" #LightBlue
    participant Communication as com
    participant Data as data
end box

server ->> com : addComment(trackId : UUID, comment : Comment)

activate com
com -> data++ : addComment(trackId : UUID, comment : Comment)

data -> data++ : getTrack(trackId : UUID) 
return Track

alt track exists
    data -> data++ : saveComment(trackId : UUID, comment : Comment)
    return void | CommentException
end

return void

@enduml